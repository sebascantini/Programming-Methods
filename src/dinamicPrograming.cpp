#include "dinamicPrograming.h"

estructura memo;

void aux(info_t &info){ // n veces
    aux(prox nodo) // o(1)
    alocar ingo en memo // o(m)
}


int dinamicPrograming(info_t &info){
    aux(info); // o(m*n)
    int ans = conseguir_max(memo); // o(m)
    return ans;
}


f(i,){
    

}

