#ifndef _BRUTEFORCE_HH_
#define _BRUTEFORCE_HH_

#include "headers.h"

int bruteForce(info_t &info, int riesgo = 0, int beneficio = 0, int actual = -2);
//la funcion arranca con actual = -2 ya que de esa manera el for arranca desde el local con indice 0;

#endif
